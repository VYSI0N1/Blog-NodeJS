extends ../layout

block content
    #wrapper
        include ../includes/sidebar
        #page-wrapper.gray-bg.dashbard-1
            include ../includes/navbar
            .row.wrapper.border-bottom.white-bg.page-heading
                .col-lg-10
                    h2 Ajouter une catégorie
                    ol.breadcrumb
                        li
                            a(href='/dashboard') Dashboard
                        li.active
                            strong Ajouter une catégorie
                .col-lg-2
            .wrapper.wrapper-content.animated.fadeIn
                .row
                    .col-lg-12
                        .ibox.float-e-margins
                            .ibox-title
                                h5 Ajouter une catégorie
                            .ibox-content
                                form.form-horizontal(method='post' action='/dashboard/posts/addcategory')
                                    .form-group
                                        label.col-sm-2.control-label Titre
                                        .col-sm-10
                                            input.form-control(type='text' name="name")
                                    .hr-line-dashed
                                    .form-group
                                        label.col-sm-2.control-label Description
                                        .col-sm-10
                                            textarea#mytextarea.form-control(name="desc" style='height: 400px;') Description de la catégorie
                                    .hr-line-dashed
                                    .form-group
                                        .col-sm-4.col-sm-offset-2
                                            button.btn.btn-primary(type='submit') Enregistrer
            include ../includes/footer
    block javascripts
        script.
            tinymce.init({
                selector: '#mytextarea'
            });


            $('.tagsinput').tagsinput({ tagClass: 'label label-primary'});

            $('.chosen-select').chosen({width: "100%", name:'cat'});