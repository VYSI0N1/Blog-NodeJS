extends ../layout

block content
    #wrapper
        include ../includes/sidebar
        #page-wrapper.gray-bg.dashbard-1
            include ../includes/navbar
            .row.wrapper.border-bottom.white-bg.page-heading
                .col-lg-10
                    h2 Ajouter une page
                    ol.breadcrumb
                        li
                            a(href='/dashboard') Dashboard
                        li.active
                            strong Ajouter une page
                .col-lg-2
            .wrapper.wrapper-content.animated.fadeIn
                .row
                    .col-lg-12
                        .ibox.float-e-margins
                            .ibox-title
                                h5 Ajouter une page
                            .ibox-content
                                form.form-horizontal(method='post' action='/dashboard/pages/add')
                                    .form-group
                                        label.col-sm-2.control-label Titre
                                        .col-sm-10
                                            input.form-control(type='text' name="title")
                                    .hr-line-dashed
                                    .form-group
                                        label.col-sm-2.control-label Contenu
                                        .col-sm-10
                                            textarea#mytextarea.form-control(name="content" style='height: 400px;') Bonjour
                                    .hr-line-dashed
                                    .form-group
                                        .col-sm-4.col-sm-offset-2
                                            button.btn.btn-primary(type='submit') Créer une nouvelle page
            include ../includes/footer
    block javascripts
        script.
            tinymce.init({
                selector: '#mytextarea'
            });


            $('.tagsinput').tagsinput({ tagClass: 'label label-primary'});

            $('.chosen-select').chosen({width: "100%", name:'cat'});